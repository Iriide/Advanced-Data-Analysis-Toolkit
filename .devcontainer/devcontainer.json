{
	"name": "Python 3",
	"image": "mcr.microsoft.com/devcontainers/python:2-3.12-bookworm",

	"postCreateCommand": "sudo apt-get update && sudo apt-get install -y libcairo2 graphviz && sudo sh -c \"curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash -s -- --to /usr/local/bin\" && pip install uv && uv sync --all-extras",

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"charliermarsh.ruff",
				"ms-toolsai.jupyter",
				"ms-azuretools.vscode-docker"
			]
		}
	},
	"features": {
		"ghcr.io/prulloac/devcontainer-features/pre-commit:1.0.3": {},
        "ghcr.io/devcontainers/features/docker-in-docker:2": {
            "version": "latest",
            "moby": true
        }
    },
    "forwardPorts": [8000]
}
